-- StockEnCasa — esquema PostgreSQL (placeholder Entrega 1).
--
-- Este archivo es un boceto referencial. La estructura definitiva se versiona como migraciones
-- numeradas dentro de db/migrations/ al iniciar el Sprint 4 (Entrega 2).
--
-- Las tablas se listan a modo de guía y NO deben ejecutarse aún:

-- CREATE TABLE users (
--   id            UUID PRIMARY KEY,
--   firebase_uid  TEXT UNIQUE NOT NULL,
--   email         TEXT UNIQUE NOT NULL,
--   display_name  TEXT,
--   created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
-- );

-- CREATE TABLE households (
--   id            UUID PRIMARY KEY,
--   name          TEXT NOT NULL,
--   invite_code   TEXT UNIQUE NOT NULL,
--   created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
-- );

-- CREATE TABLE household_members ( ... );
-- CREATE TABLE categories ( ... );
-- CREATE TABLE products ( ... );
-- CREATE TABLE shopping_list_items ( ... );
-- CREATE TABLE notifications ( ... );
