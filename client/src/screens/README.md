# screens/

Pantallas previstas para Entrega 2 (alineadas con la sección 10 de `CLAUDE.md`):

- `OnboardingScreen` — bienvenida + creación/unión a hogar (HU-11).
- `HomeScreen` — inventario agrupado por categoría (HU-04).
- `ScanScreen` — cámara con segmented control "Barcode / OCR" (HU-01, HU-02).
- `ConfirmProductScreen` — confirmación pre-completada (decisión UX: "la IA sugiere, el usuario decide").
- `ShoppingListScreen` — lista compartida con voz y barra de progreso (HU-07, HU-08, HU-10).
- `ProfileScreen` — miembros del hogar y settings.
